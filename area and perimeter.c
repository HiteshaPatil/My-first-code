#include<stdio.h>
int main()
{
  int square,side;
  printf("Enter side of square:");
  scanf("%d",side);
  area=side*side;
  perimeter=4*side;
  printf("area=%d",area);
  printf("square=%d",square);
  return 0;
}
